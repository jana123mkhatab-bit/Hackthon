# Server-only keys. Never prefix these with NEXT_PUBLIC_.
# Gemini powers all AI features; keep this key server-side.
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-3.6-flash

# MongoDB Atlas (use the SRV connection string from Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.raehh7f.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=qlsahmedothman
